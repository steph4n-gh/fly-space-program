# Frozen orbital evaluation and recorded-action audit

This method and its source are prepared before either completed evaluation report is opened or interpreted. They apply only to the six matched normal-mode cases frozen in plan `ecd27042dae78467a42111660e44e5da4d1fa8717852118626d920a7a6fdb8a7`. Both original evaluation sessions are now terminal: control 86855 and conditional_periapsis 48145, each exit 0. The root receipt `original-evaluation-terminal-confirmation.json`, SHA256 `bb4da37e3d1273af356027335e131b375b4e649c23a3bf1fe0da7e93a01e4f5d`, records these observations and the byte hashes of both reports and weight files. Receipt and static source inspection did not inspect any evaluation outcome.

The new audit is separate from the completed training audit. It does not change, repeat, or reinterpret the training-selection procedure. Source and method hashes must be reviewed by the root before execution and supplied explicitly as arguments. There is no preflight mode that reads outcomes. Syntax checking alone is permitted before review.

1. Before opening either report, verify the audit source/method/helper hashes and the exact root receipt, including both original session IDs, original-attempt flags, no-restart flag and exit codes. Create the fixed `replay` output directory exclusively. Its existence prevents a second invocation, including after a failed first invocation. No alternate output directory or retry option exists.
2. Check the frozen evaluation execution manifest `443fa06996ee9bd763bfc9fd65043c905bb05ccf7fc90864a9b0edd595c49d5a`, its complete input hash map, all 72 runtime/resource/native/graph hashes, the exact executable binary/version and absence of Node preload overrides. Check both original launch receipts against the prescribed commands, working directory, explicit environment and stronger recorded complete clean environment. Preserve the historical training-environment limitation from the execution freeze; prospective evaluation provenance does not retroactively establish missing historical training evidence.
3. Verify each generation-4, 288-episode final candidate against the completed training freeze, the explicit Float64 little-endian 28-parameter digest and 21,300-weight binary digest. Check each report and evaluation-weight file against the terminal receipt. Require unchanged parameters, weights, source hashes, calibration, native build, correct per-arm progress mode, exactly the six prescribed cases, normal mode only, and `complete:true`. Index flights by scenario/seed/variability/mode rather than asynchronous arrival order. Reject duplicates, omitted cases, extra conditions, malformed endpoints (including complete nested variation, touchdown and milestone schemas, finite values, unique chronological original milestones, and style-off fields), censored flights, incomplete trajectory dimensions, nonfinite action/record vectors, and nonmonotonic decision times. Validate all twelve full traces before importing physics or creating any flight state. Write a separate completion-audit artifact with zero physical replays started at this point.
4. Import only the frozen physics module's `createFlight`, `advance`, and `decisionSteps`, plus the already frozen independent scalar `presented.mjs` helper. These imported physics modules also contain dormant controller/guidance definitions; the audit calls none of them. It loads no network, worker, sampler or neural inference and does not fit or edit any controller. Exactly twelve physical states are created, one per recorded arm/case even if both selected controllers or trajectories are identical. No case can be replaced or skipped.
5. At each recorded decision compare all seven stored physical state values and all 28 reconstructed presented instrument/body values exactly, after the same JSON number serialization used by the original recorder. This treats serialized negative zero as zero and introduces no tolerance. Retain all original recorded actions, presented values and decoded values. Advance only the recorded ten-component action for the exact `decisionSteps` cadence until the original endpoint, retaining the original 50/250-ms physical steps. Count every attempted `advance` and every successful return immediately, before computing diagnostics or writing step output. Require every physical endpoint field, milestone, reason, time, score and touchdown value to match the original serialized endpoint exactly. Decoded values are retained as recorded measurements; this audit does not recompute graph inference or add motor-head decomposition.
6. Reconstruct orbital radius, energy, angular momentum, semimajor axis, eccentricity, periapsis and apoapsis from each original post-step state, preserving the frozen arithmetic order. Export unbound and nonfinite values explicitly as strings such as `Infinity`, never silently as null. Retain all three normalized deficits from the old reward and their physical-unit deficits: periapsis shortfall below 800 m, apoapsis-error excess beyond 200 m, and absolute-vertical-speed excess beyond 5 m/s. Export every physical state, action and deficit; summarize the minimum, maximum, time integral, positive duration and nonfinite duration for each physical deficit. The predicate can fail exactly at a strict boundary despite a zero deficit; retain the separate predicate booleans and all eight three-predicate pattern durations.
7. Independently recompute the frozen outcome-only H/P measurements from the complete post-step sequence. Eligible steps have finite periapsis, finite apoapsis strictly within 200 m of destination and finite absolute vertical speed strictly below 5 m/s. Current hold grows by the exact original dt only when additionally periapsis is strictly above 800 m; otherwise reset it to zero. Track longest hold, total qualifying duration, eligible duration and maximum eligible periapsis using the frozen `max(previous ?? -6000, periapsis)` arithmetic. Keep no-eligible periapsis null, and compute its P using the prescribed -6000 floor. H is min(3,longest)/3; P is (max(-6000,min(800,best ?? -6000))+6000)/6800. Compare every recorded H/P field exactly. These measurements never set simulator phase or milestone flags. Retain first eligible/qualifying times, the conditional-periapsis peak and longest-hold end.
8. Recompute the old three-second rolling quality twice. Reproduce the original queue operation order for exact recorded-quality and reward equality. Separately sum the overlap of each physical interval with the trailing three-second window. Require independent maximum-quality error <=1e-11 and reward error <=2e-8, fixed from the earlier paired-analysis source before these outcomes were read. Retain every rolling integral, quality and running maximum plus the peak physical state. Reproduce the archived instantaneous proxy at each original decision endpoint, including nonfinite handling and original arithmetic order, and require its quality/reward to match exactly. Derive the unchanged per-flight fitness from score, old rolling reward and original touchdown margin; the evaluation report itself contains no top-level fitness field.
9. Preserve all twelve complete case summaries, original decisions, physical steps, endpoints and six explicit conditional-minus-control pairs. Report each arm's original milestone counts, complete-orbit flags, landings, mean H/P and longest hold, all no-eligible cases, conditional periapsis values, original rewards/fitness, elapsed flight time and every failed reason. Retain all milestones, including deorbit, atmospheric entry and final approach, without redefining a new return-success predicate. Means include all six cases; no metric, trial or failure is discarded. Provide JSON, JSONL and per-case CSV artifacts. The pair artifacts retain both complete cases and explicitly test whether the two recorded sequences and original physical endpoints are identical.
10. Preserve case start/completion records and incremental physical-step/decision/case files. Count each fully checked physical replay before any subsequent case-artifact write. An error removes any published replay PASS summary and records the active case, all counters, known input identities and the error; it aborts without replacement, retry or continuation. Successful completion additionally requires 12 starts, 12 created states, 12 completed exact endpoints, matching attempted/successful step counts and unchanged before/after hashes for every tracked input. Hash the completed outputs and publish replay `summary.json` with PASS only as the final operation. A completion-audit PASS before replay is explicitly scoped to input completeness and is not a replay PASS.

Only files below `/tmp/orbital-progress-evaluation-audit` are written by this audit. The successful replay directory is single-use. The budget is exactly twelve recorded-action physical replays and zero new neural flights; a failure may leave fewer completed replays and must retain that shortfall. No training restart, new seed, coefficient replacement, guidance, positive control, extra evaluation, release promotion or repository edit is authorized here.

The hypothesis is evaluated against every original outcome and both arms, not solely against a learned metric. P improvement without qualifying H or an original insertion milestone remains failed insertion capability. One optimizer seed and six development cases cannot establish optimizer superiority, general reliability, ground retention or release eligibility; independent JavaScript testing is still required for a release claim.

Invocation after root review:

`/opt/homebrew/Cellar/node/24.3.0/bin/node /tmp/orbital-progress-evaluation-audit/audit.mjs AUDIT_SOURCE_SHA256 METHOD_SHA256`
